version: 2

models:
    - name: metrics_login_activity_status_28d
      description: This model is exactly the same as the one described in `metrics_login_activity_status`, but referring to monthly active users instead.
