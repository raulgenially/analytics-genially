version: 2

models:
  - name: recurrent_publishers
    description: see corresponding definition in macros.
      This model is a filtered version of the users model and contains the same fields.
