version: 2

models:  
    - name: dim_users
      columns:
        - name: user_id
          description: Primary key for users.
          tests:
            - unique
            - not_null

        - name: plan
          description: "{{ doc('subscription_codes') }}"
          tests:
            - not_null
            - accepted_values:
                values: ['Free', 'Pro', 'Master', 'Edu Pro', 'Edu Team', 'Team', 'Student']

        - name: sector
          description: Sector (see data/new_sector_codes.csv and data/old_sector_codes.csv).
          tests:
            - not_null

        - name: role
          description: Role (see data/new_role_codes.csv and data/old_role_codes.csv).
          tests:
            - not_null

        - name: market
          description: Country.
          tests:
            - not_null

        - name: n_total_creations
          description: Total number of creations (including those that has been removed).
          tests:
            - not_null

        - name: n_active_creations
          description: Total number of active creations (excluding those that has been removed).
          tests:
            - not_null

        - name: is_validated
          description: Whether the user has validated their account.
          tests:
            - not_null

        - name: first_creation_at
          description: Date at which the first genially (removed or not) was created.

        - name: last_creation_at
          description: Date at which the last genially (removed or not) was created.

        - name: registered_at
          description: Registration date.
          tests:
            - not_null

        - name: last_access_at
          description: Last access date.
          tests:
            - not_null