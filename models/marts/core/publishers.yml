version: 2

models:
  - name: publishers
    description: Users that have published at some point a Genially (including users that act as collaborators).
      This model is a filtered version of the users model and contains the same fields.
